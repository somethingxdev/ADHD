---
import Card from './starwind/card/Card.astro';
import { CardHeader, CardTitle, CardDescription, CardContent } from './starwind/card/index.ts';
import data from '@/data/signs.yaml';

const items = data.items;
---

<section class="py-10 lg:py-20">
  <div class="container">
    <h2 class="text-3xl md:text-4xl lg:text-5xl mb-6 lg:mb-12 font-semibold">{data.title}</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3">
      {
        items.map((sign: { title: string; description: string }) => (
          <Card>
            <CardHeader>
              <CardTitle>{sign.title}</CardTitle>
            </CardHeader>
            <CardContent>
              <CardDescription>{sign.description}</CardDescription>
            </CardContent>
          </Card>
        ))
      }
    </div>
  </div>
</section>
